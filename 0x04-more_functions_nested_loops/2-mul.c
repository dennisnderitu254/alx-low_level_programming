#include "main.h"
/**
 *mul - function that multiplies two numbers
 *@a: first number
 *@b: second number
 *Return: returns result
 */

/*
1. It’s defining a function called mul that takes two integers as arguments and returns an integer.
2. It’s returning the product of the two arguments.
*/
int mul(int a, int b)
{
return (a * b);
}
